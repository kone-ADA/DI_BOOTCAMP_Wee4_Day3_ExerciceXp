🌟 Exercise 1 : Simple Exercise Using Angular And RxJS
Instructions
Create an Angular component that displays a list of user names.

The list of user names should be retrieved from a REST API endpoint that returns an array of user objects.

Each user object has a name field.

Use RxJS to handle the HTTP request and response, and to display the user names in the component template.


🌟 Exercise 2 : Observable To Track Focus In A Section Of The Page
Instructions
Create an Angular component with 
a template that includes 
a div element with the class focus-section.
Use RxJS to create an Observable that emits a value every time the focus-section element receives focus.

🌟 Exercise 3 : Create An Angular Service
Instructions
Create an Angular service class that has a method that logs a message to the browser console. Inject the service in a component and use the method to log a message when a button is clicked.

🌟 Exercise 4 : Angular Services And Dependency Injection
Instructions
1-Create a new Angular project using the Angular CLI.

2-Create a new service called MessageService that has a method called getMessage() that returns a string message.

3-Inject the MessageService into the root component of your Angular application.

4-Use the getMessage() method in the template of the root component to display the message on the page.

5-Test the application to make sure the message is displayed correctly.